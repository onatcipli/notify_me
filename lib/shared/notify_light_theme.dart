import 'package:flutter/material.dart';


// TODO: Add other color that we will use in our app.
const Color primaryColorDark = Color(0xff222831);
const Color primaryColor = Color(0xffF5F5F5);
const Color primaryColorLight = Color(0xff3FC1C9);
const Color primarySwatch = Color(0xfffce38a);

final lightTheme = ThemeData(
  primaryColorDark: primaryColorDark,
  primaryColor: primaryColor,
  primaryColorLight: primarySwatch,
);
